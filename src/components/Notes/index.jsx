import Note from "../Note";

import "./notes.scss";

export default function Notes() {
	return (
		<section className='notes'>
			<Note />
			<Note />
			<Note />
			<Note />
			<Note />
			<Note />
		</section>
	);
}
